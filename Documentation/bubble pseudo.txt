READ array
SET temp variable as zero

foreach item in array	
	foreach item in array
		set sort as zero
		if sort is less then array length
		sort plus one
	
		IF array index_sort is greater then array_sort plus one
			set temp variable as array index_sort plus one
			set array_sort plus one as array_sort
			set array_sort as temp variable